# Work Log — 2026-06-24

Everything done in this session, grouped by area. Nothing committed yet; see
"Still open" at the end.

## 1. Repo cleanup
- **73 of 118 tracked files were macOS `._` junk** (exFAT artifact), plus
  `.DS_Store` and Python caches.
- Removed 53,778 `._*` files from disk; untracked all junk and `__pycache__`/`.pyc`
  from git (tracked files 118 → 40).
- Wrote a proper root `.gitignore`; fixed the wrong path in `backend/.gitignore`.
- Restored `backend/requirements.txt`.
- `.venv` and `backend/venv` are broken on exFAT (can't hold symlinks) -- switched
  all work to anaconda `python3`.

## 2. Status check
- Cross-checked the checklist against the repo. Most "done" work (Chroma, `/search`,
  `/ask`, frontend) was never saved to the repo.
- Wrote `docs/STATUS.md` with the real state, dead URLs, and true next steps.

## 3. Backend build
- **Download:** re-ran `download_new_sources.py`; found 10 dead URLs; confirmed 18
  valid pages.
- **Loader:** wrote `backend/ingest/load_to_chroma.py` from scratch (HTML→text,
  chunking, MiniLM embeddings, Chroma `rwr_docs`) → 59 chunks.
- **`/search`:** built `backend/services/retriever.py` + `backend/api/search.py`,
  wired CORS into `backend/main.py`; verified retrieval.
- **`/ask`:** built `backend/api/ask.py` with CLINIC guardrails (grounded-only,
  citations, no individualized-advice phrasing, disclaimer, refuses
  immigration/criminal/family/danger with hotlines). Model: Sonnet 4.6 with adaptive
  thinking.
- **Benchmark:** wrote `data/eval/benchmark.json` + `scripts/eval.py`; first run
  12/17 (70.6%), 0 forbidden-language hits, 0 missing disclaimers.
- Created `backend/ingest/__init__.py` and `backend/api/__init__.py`; filled in
  `backend/.env.example`.

## 4. Security fix
- Real `ANTHROPIC_API_KEY` had landed in the git-tracked `.env.example`. Scrubbed
  back to a placeholder; key now lives only in the gitignored `backend/.env`.

## 5. Zip merge
- The zip was an earlier fuller snapshot, not a duplicate.
- Merged in missing housing (tenant-rights, `chicago-rlto.pdf`) and benefits
  (SNAP/Medicaid/TANF/LIHEAP/AllKids) content + `parse_html.py` / `parse_pdf.py`;
  added PDF support to the loader.
- Excluded immigration content, the older JSON loader, and all junk.
- Re-ran loader → 117 chunks; benchmark → 16/21 (76.2%).

## 6. Retrieval tuning
- Added a chunks-per-source cap (`MAX_PER_SOURCE=2`, over-fetch x6) to
  `retriever.py` so one long page can't flood top-k. Recovered repair-05: 76.2% → 81.0%.
- Corrected a mislabeled benchmark test (repair-04) → 18/21 (85.7%).
- Remaining 3 failures (debt-04, debt-05, repair-07) are genuine retrieval misses:
  thin/lexically-distant chunks MiniLM never surfaces. Needs better source content
  or hybrid keyword+vector search.

## 7. Sources tracker
- Wrote `scripts/update_sources_tracker.py` (re-runnable) to derive per-source
  status from Chroma + filesystem.
- Refreshed `data/rights_sources.csv`: added status, chunk_count, date_ingested,
  last_checked; expanded 29 → 42 sources. Health: 22 live, 6 thin, 3 empty, 11 dead.

## 8. Frontend
- Added the designed frontend: 7 react-router pages, components, icon set, logo
  SVGs, accessibility wiring. Replaced the Vite skeleton; installed
  `react-router-dom` + `vite-plugin-svgr`. Build passes.
- Fixed `/api` URL mismatch in `lib/api.ts`.
- Adapted the backend to match the frontend's `/ask` contract: `sources[]` always
  a list, top-level `topic`, structured `refusal_org` on refusals.
- Updated `scripts/eval.py` to require the disclaimer only on substantive answers.
  Benchmark held at 18/21 (85.7%).

## 9. More sources
- ILAO plain-language pages are the best source type; ilga.gov statute URLs now 404.
- Wrote `data/new_sources.csv` (11 vetted) + `scripts/fetch_sources.py`. Downloaded
  10 live; content-rich (ILAO payday 18k chars, debt 15k, security deposit 9k).
- Ingested → 187 chunks. Tightened `MAX_PER_SOURCE` 2 → 1.
- repair-07 fixed; debt-04/05 now answered by correct ILAO pages. Benchmark 20/21
  (95.2%). One new minor regression: debt-07 (broad ILAO pages crowd out CARPLS).
- Fixed a slug-matching bug in `update_sources_tracker.py`. Tracker: 32 live, 6
  thin, 3 empty, 10 dead / 51 sources.
- YOTUO external drive unmounted mid-operation and remounted; files intact. Strong
  signal to move off exFAT.

## Result snapshot

| Metric | Start | End |
|---|---|---|
| Tracked files | 118 (73 junk) | 40 (clean) |
| Chroma chunks | 0 | 187 |
| Topics | -- | housing, benefits, money_debt, housing_repair |
| Endpoints | `/health` only | `/health`, `/api/search`, `/api/ask` |
| Benchmark | -- | 20/21 (95.2%) |
| Sources | -- | 51 (32 live, 6 thin, 3 empty, 10 dead) |

## Still open
- 3 benchmark failures (debt-04, debt-05, repair-07) -- genuine retrieval misses;
  needs fuller source text or hybrid keyword+vector search.
- 3 pages extract 0 text (JS-rendered: ComEd x2, Solar); HUD-203k and a few stubs
  are thin; IHDA HRAP/HAFHR are duplicate content.
- 10 dead source URLs to refresh.
- `benefits-immigrants.html` and `snap-non-citizens.html` touch immigrant eligibility
  -- review against scope decision.
- Frontend visual identity (font + palette via Canva) is owner's task.
- Nothing committed yet. Sweep `._` junk before committing:
  `find . -path ./.git -prune -o -name '._*' -delete`