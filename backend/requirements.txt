# Backend runtime dependencies.
# Install: pip install -r backend/requirements.txt

# web framework
fastapi
uvicorn[standard]
slowapi          # per-IP rate limiting
python-dotenv

# retrieval / RAG
chromadb         # vector store (data/chroma, collection "rwr_docs")
sentence-transformers  # local embeddings (all-MiniLM-L6-v2) + optional CrossEncoder rerank
rank-bm25        # optional hybrid dense+BM25 retrieval (only used when HYBRID_SEARCH=1)

# LLM
anthropic        # /ask answering + question translation

# ingestion
beautifulsoup4
pdfplumber
requests

# tests
pytest

# not used yet -- re-add when implemented
# psycopg2-binary sqlalchemy pgvector  -- switched to Chroma
# deepl google-cloud-translate         -- Phase 4 translation
# redis                                -- Phase 4 cache