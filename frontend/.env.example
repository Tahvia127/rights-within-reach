# API URL for the Rights Within Reach FastAPI backend.
# When running locally, the backend is typically at http://localhost:8000.
# In production, set this to your deployed API origin.

VITE_API_URL=http://localhost:8000
