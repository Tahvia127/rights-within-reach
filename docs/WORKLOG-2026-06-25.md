# Work Log — 2026-06-25

Continues from WORKLOG-2026-06-24.md. **Project is now on local disk at
`~/rights-within-reach`** (moved off the failing YOTUO external drive).

## 1. Moved off the external drive
- YOTUO unmounted mid-write twice. Copied the project to `~/rights-within-reach`
  via rsync (skipped node_modules/.venv/caches). Git, 187 chunks, and all source
  files verified intact.

## 2. Fixed chat answers showing raw markdown
- Answers were rendering literal `##`, `**`, `[5]`, `---` as text.
- Backend (`ask.py`): rewrote the system prompt -- no `[n]` citation markers, no
  headings, plain short paragraphs, bold only for key numbers, direct answer first.
- Frontend (`Chat.tsx`): added `react-markdown` so bold/lists/dividers render
  correctly. Build passes.
- Verified: answers are clean. Benchmark steady (~90%).

## 3. Source hunt across all topics
- ~16 web searches across housing, money/debt, home repair, benefits, resources.
  ILAO plain-language pages are still the best source type.
- Cataloged **52 vetted sources** in `data/new_sources.csv`. Downloaded **49 live**
  (1 dead ILAO URL, 2 thin/redirect) via `scripts/fetch_sources.py`.
- Added `resources` topic to `RAW_DIRS` in the loader.
- Ingested -- corpus grew from **187 to 563 chunks**. By topic: housing 203,
  money_debt 178, benefits 103, housing_repair 65, resources 14.
- Benchmark held at **18/21 (85.7%)** after 3x corpus growth.
- Tracker refreshed: **99 sources** (71 live, 6 thin, 11 empty, 11 dead).

## Still open
- **Translation**: have Claude answer in the target language (`language` already
  sent from api.ts); fill i18n tables in `translations.tsx`; native review.
- **Mobile nav**: sticky bottom nav across all 7 pages.
- **FastAPI hardening**: Pydantic response models, async, rate limiting, error handling.
- **Database**: Chroma + SQLite now; Supabase (Postgres+pgvector) at deploy.
- **money_debt benchmark**: broad ILAO pages crowd out specific statutes for
  debt-04/05/07. Consider a hybrid keyword+vector retriever.
- 1 dead + 2 thin + 11 empty sources to revisit.
- Nothing committed yet (pending review).